const { About } = require('./About');
window.customElements.define('mf-about', About);

const { Footer } = require('./Footer');
window.customElements.define('mf-footer', Footer);

const { Home } = require('./Home');
window.customElements.define('mf-home', Home);

const { Nav } = require('./Nav');
window.customElements.define('mf-nav', Nav);

const { NotFound } = require('./NotFound');
window.customElements.define('mf-not-found', NotFound);
